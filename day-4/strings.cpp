#include<iostream>
#include<string>
using namespace std;
int main()
{
    string str="hello";
    // for(int i=0;i<str.length();i++)
    // {
    //     cout<<str[i]<<" ";
    // }
    // cout<<str.length()<<endl;

    // cout<<str.append("world");

    // cout<<str.substr(0,2);
    cout<<str.find("l");
    return 0;
}

// use getline(cin,str_name) for using the space inside the input when we are taking from the user
